package com.tka;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;

public class Student {

	public void insertData() throws ClassNotFoundException, SQLException {
		//1.Register and Load Driver Class
		
		Class.forName("com.mysql.cj.jdbc.Driver");
		System.out.println("Driver Class Loaded Successfully...!");
		
		//2. get Connections Using Connection(I)
		Connection con = DriverManager.getConnection("jdbc:mysql://localhost:3306/batch426","root", "Sonu@2002");
		
		// 3. Used Statement & PreaperdStatement
		 java.sql.Statement s = con.createStatement();
				 
		//4.Generate Queries
		 int checked = s.executeUpdate("insert into Student_Info(id,name,city)values(127,'Sonal','Goa')");
			 if(checked>0) {
					 System.out.println("Data inserted...!");
				 }else {
					 System.out.println("Data Not Inserted...!");
				   }
			 
			 con.close();
	}
	
		

			public void deleteData() throws ClassNotFoundException, SQLException {
				//1.Register and Load Driver Class
				
				Class.forName("com.mysql.cj.jdbc.Driver");
				System.out.println("Driver Class Loaded Successfully...!");
				
				//2. get Connections Using Connection(I)
				Connection con = DriverManager.getConnection("jdbc:mysql://localhost:3306/batch426","root", "Sonu@2002");
				
				// 3. Used Statement & PreaperdStatement
				 java.sql.Statement s = con.createStatement();
						 
				//4.Generate Queries
				 int checked = s.executeUpdate("delete from Student_Info where id=126");
					 if(checked>0) {
							 System.out.println("Data deleted...!");
						 }else {
							 System.out.println("Data Not deleted...!");
						   }
				 con.close();
			
				
	}
			
			public void updateData() throws ClassNotFoundException, SQLException {
				//1.Register and Load Driver Class
				
				Class.forName("com.mysql.cj.jdbc.Driver");
				System.out.println("Driver Class Loaded Successfully...!");
				
				//2. get Connections Using Connection(I)
				Connection con = DriverManager.getConnection("jdbc:mysql://localhost:3306/batch426","root", "Sonu@2002");
				
				// 3. Used Statement & PreaperdStatement
				 java.sql.Statement s = con.createStatement();
						 
				//4.Generate Queries
				 int checked = s.executeUpdate("update Student_Info set name ='Payal' , city='Pune' where id=125");
					 if(checked>0) {
							 System.out.println("Data Updated...!");
						 }else {
							 System.out.println("Data Not Updated...!");
						   }
				 con.close();
				
	}
}
